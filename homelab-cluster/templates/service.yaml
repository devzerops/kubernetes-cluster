apiVersion: v1
kind: Service
metadata:
  name: {{ include "homelab-cluster.fullname" . }}
  labels:
    {{- include "homelab-cluster.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "homelab-cluster.selectorLabels" . | nindent 4 }}
